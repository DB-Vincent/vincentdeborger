<!DOCTYPE html>
<html lang="en-US">
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" type="text/css" href="{{asset "built/screen.css"}}" />

        <title>{{meta_title}}</title>

        {{ghost_head}}
        {{!-- Outputs important meta data and settings, should always be in <head> --}}
    </head>
    <body class="dark:bg-black dark:text-white relative">
    {{!--         {{navigation}} --}}

        <div class="flex h-screen justify-center items-center">
            <div class="flex flex-col items-center">
                <h1 class="font-bold text-blue-500 text-9xl">{{statusCode}}</h1>

                <h6 class="mb-2 text-2xl text-center text-gray-500 md:text-3xl">
                    <span class="text-blue-500">Oops!</span> {{message}}
                </h6>

                <p class="mb-8 text-center text-gray-500 md:text-lg">
                    <ul>
                    {{#foreach errorDetails}}
                        <li>
                            <h5 class="text-lg font-semibold">{{{rule}}}</h5>

                            {{#foreach failures}}
                                <strong>Ref:</strong> {{ref}}<br>
                                <strong>Message:</strong> {{message}}
                            {{/foreach}}
                        </li>
                    {{/foreach}}
                    </ul>
                </p>

                <p class="text-sm font-semibold text-gray-500"><a href="{{@site.url}}">Go to the front page →</a></p>
            </div>
        </div>
    </body>
</html>





